{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ \"${VERCEL_ENV:-}\" = \"production\" ]; then python backend/manage.py migrate --noinput; fi && npm --prefix frontend ci && VITE_API_URL=${VITE_API_URL:-same-origin} npm --prefix frontend run build:prod && node frontend/scripts/sync-spa.js",
  "functions": {
    "backend/config/wsgi.py": {
      "maxDuration": 300,
      "memory": 1024
    }
  },
  "crons": [
    { "path": "/api/internal/cron/recurring-deadlines/", "schedule": "0 2 * * *" },
    { "path": "/api/internal/cron/deadline-scan/", "schedule": "30 2 * * *" }
  ]
}
